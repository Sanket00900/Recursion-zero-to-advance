#include <bits/stdc++.h>
using namespace std;
void f(int i,vector<int>&ds,int arr[],int n){
    if(i==n){
        for(auto itr:ds){
            cout<<itr<<" ";
        }
        if(ds.size()==0)cout<<"{}";
        cout<<endl;
        return;
    }
    
    ds.push_back(arr[i]);
    f(i+1,ds,arr,n);//take it and print it
    ds.pop_back();
    f(i+1,ds,arr,n);//not take it 
}
int main() {
	
	int arr[]={3,1,2};
	int n=3;
	vector<int>ds;
	f(0,ds,arr,3);
	return 0;
}

Very very useful code snippet for writing any recursion code related to sub sequence just run it and draw a recursion tree for better idea!!
If any doubt arise feel freee to Comment down below!
